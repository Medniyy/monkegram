# Used ONLY by scripts/fetch-metadata.ts (one-time, local).
# The deployed app needs NONE of these — it reads static JSON.

# Free key from https://dev.helius.xyz  (this is the ONLY thing you must fill in)
HELIUS_API_KEY=

# Verified on-chain collection mints — already baked into the fetch script as
# defaults (confirmed from on-chain metadata). Only set these to override.
# GEN2_COLLECTION=SMBtHCCC6RYRutFEPb4gZqeBLUZbMNhRKaMKZZLHi7W
# GEN3_COLLECTION=8Rt3Ayqth4DAiPnW9MDFi63TiQJHmohfTWLMQFHi4KZH
